plugins {
    id 'java'
}

group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

test {
    def parallelTests = project.hasProperty('parallelTests')
    useTestNG() {
        suites "src/test/testng.xml"
        if (parallelTests) {
            parallel 'tests' // Set to 'tests' mode for parallel execution by tests
        }
    }

}

dependencies {
    testImplementation group: 'org.testng', name: 'testng', version: '7.7.1'
    testImplementation group: 'io.appium', name: 'java-client', version: '9.4.0'
}
